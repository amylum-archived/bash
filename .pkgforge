name 'bash'
org 'amylum'

license 'COPYING'

configure_flags(
  prefix: '/usr',
  'without-bash-malloc': nil
)

harden

patch '0001-help-fix-printf-format-security-warning.patch'

build do
  configure
  make
  install
end

test do
  run 'bash --version'
end
